{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "realease": "turbo run build --filter=@digimo-ui/docs && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.25.1",
    "turbo": "^1.6.2"
  }
}